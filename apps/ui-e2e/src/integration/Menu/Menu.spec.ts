describe('ui: Menu component', () => {
  beforeEach(() => cy.visit('/iframe.html?id=layout-menu--primary'))

  it('should render the component', () => {
    // cy.get('h1').should('contain', 'Welcome to ui!')
  })
})
