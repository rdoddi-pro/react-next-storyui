import React, { FC } from 'react'
import styles from './Inner.module.less'

const InnerMedicalFormRequired: FC = () => {
  return <div className={styles.requiredDiv}>required</div>
}

export default InnerMedicalFormRequired
